# URL base da API do backend my-money (sem barra final, sem prefixo /api).
# Em desenvolvimento as chamadas passam pelo proxy configurado em vite.config.js,
# então este valor só é usado de fato no build de produção (baseURL do axios).
VITE_API_URL=http://localhost:3333
